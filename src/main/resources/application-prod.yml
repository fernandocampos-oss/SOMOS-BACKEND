# Configuración para producción (usa Config Server)
spring:
  config:
    import: configserver:${CONFIG_SERVER_URL:https://appsqa.essalud.gob.pe/config-service}
  cloud:
    config:
      username: ${CONFIG_SERVER_USERNAME}
      password: ${CONFIG_SERVER_PASSWORD}
